#include <stdio.h>

int main()
{
    // Tauhid
    int mid_term_mark = 63;
    int final_term_mark = 85;

    int total_mark = mid_term_mark + final_term_mark;
    float point = 1.0 * total_mark * (4 / 80);

    printf("Total mark: %d\n", total_mark);

    return 0;
}