#include <stdio.h>
int main()
{
    int a = 50, b = 60, sum;
    sum = a + b;
    printf("%d + %d = %d", a, b, sum); // what is the output
    printf("%d + %d = %d", b, a, sum); // what is the output
    return 0;
}
