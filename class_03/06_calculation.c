#include <stdio.h>

int main()
{
    int x = 50, y = 10, z, a, b, c, r; // variable declaration

    c = x + y;
    z = x - y;
    a = x * y;
    b = x / y;

    r = x % y; // Modulus operator

    printf("%d\n", c); // 60
    printf("%d\n", z); // 40
    printf("%d\n", a); // 500
    printf("%d\n", b); // 5

    // +, -, *, / -> Operator -> Arithmetic Operator

    // Arithmetic Operator
    // +, -, *, /, %

    // single line comment

    /*
    multiple 
    line 
    comment
    */

    return 0;
}