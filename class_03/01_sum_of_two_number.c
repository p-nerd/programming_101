#include <stdio.h>

int main()
{
    int x, y, z; // define Variable, x, y, z

    x = 12; // x variable 10 assign
    y = 20; // y variable 20 assign

    z = x + y; // sum of x, y assign korci z

    printf("The sum is %d\n", z); // output the result in console.

    return 0;
}