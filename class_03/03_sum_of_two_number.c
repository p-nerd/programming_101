#include <stdio.h>

int main()
{
    int x = 12, y = 30, z; // define Variable, x, y, z

    z = x + y; // sum of x, y assign korci z

    printf("The sum is %d\n", z); // output the result in console.

    return 0;
}